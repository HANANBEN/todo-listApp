/*

const sums =(a,b) => {
    console.log("sum is :" , a + b) ;
  
  };
  
  const executeAction = action => {
    const newAction = (...args) => {
     console.log("start execution",args)
     action(...args);
     console.log("end execution",args);
  
    }
    return newAction;
  }
  const newAction = executeAction(sums);
  newAction(1,6,5);
*/
const useDispatchaction = (action) => {};
